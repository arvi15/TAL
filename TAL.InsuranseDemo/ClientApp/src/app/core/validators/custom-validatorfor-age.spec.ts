import { CustomValidatorforAge } from './custom-validatorfor-age';

describe('CustomValidatorforAge', () => {
  it('should create an instance', () => {
    expect(new CustomValidatorforAge()).toBeTruthy();
  });
});
